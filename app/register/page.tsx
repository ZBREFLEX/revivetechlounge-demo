import { RegisterForm } from '@/components/forms/register-form'

export const metadata = {
  title: 'Register - Store Admin Dashboard',
  description: 'Create a new store admin account',
}

export default function RegisterPage() {
  return <RegisterForm />
}
