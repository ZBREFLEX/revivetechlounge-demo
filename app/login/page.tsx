import { LoginForm } from '@/components/forms/login-form'

export const metadata = {
  title: 'Login - Store Admin Dashboard',
  description: 'Sign in to your store admin account',
}

export default function LoginPage() {
  return <LoginForm />
}
